 
//======================I Werjsa dziala dla danych 22+, nie skonczona=====
 /*var stack = []
    var array = []
    function count(mathOperation){
    
        array = mathOperation.split(" ")
        console.log(array)
        array.forEach(rpn)
    }

    function rpn(value){
        
       // let value = input.shift()
        
        if(value == +value){
            x = parseInt(value)
            stack.push(x)
            console.log(stack)
        }else{ 

            console.log(stack)
            let number1 = stack.shift()
            let number2 = stack.shift()
            
            stack.push(eval(number1+value+number2))
            console.log(stack)
    
        }  
        
    }*/
//=========II Wersja dziala dla danych(2 + 2 - 1), pod warunkiem spacji,===
/* nie jestem zadowolony z tego zwraca mi undefinied pod koniec nwm czemu i sprawdzanie 3 warunków if jest wbrew DRY tak mi się zdaje*/
var stack = []
    var array = []
    var result = 0

    function count(mathOperation){
        
        //Array.from(mathOpertaion) lepsze jesli bez spacji
        array = mathOperation.split(" ")
        console.log(array)
        array.forEach(pars)
        array.forEach(rpn)
    }   

    function pars(value){
        if(value == +value){
            stack.push(parseInt(value))
        } 

    }

    function rpn(value){
        if(isNaN(value)){

             if(stack.length == 1){
                let number1 = stack.shift()
                result = eval(result+value+number1)
               }

            if(stack.length>1){  
                let number1 = stack.shift()
                let number2 = stack.shift()
                result = eval(number1+value+number2)
            }else{
               console.log("Wynik to: "+result)
            }
        }
    } 